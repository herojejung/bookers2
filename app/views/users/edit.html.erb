<h1>User Info</h1>
  <%= form_with model: @user do |f| %>
    <h4>name</h4>
      <%= f.file_field :title %>
    <h4>image</h4>
      <%= f.file_field :image, accept: "image/*" %>
    <h4>introduction</h4>
      <%= f.text_area :caption %>
    <%= f.submit 'Update User' %>
  <% end %>